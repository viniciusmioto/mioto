# CARTO Basemaps key. This is intentionally public at runtime because Leaflet
# requests tiles directly from the visitor's browser. Restrict the key in CARTO
# to this site's domains and do not grant it access to any other CARTO APIs.
NEXT_PUBLIC_CARTO_BASEMAP_KEY=
